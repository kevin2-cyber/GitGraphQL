import 'package:flutter/material.dart';

class AppConstants {
  // .env file path
  static const filePath = 'assets/.env';

  // api url
  static const baseUrl = 'https://api.github.com/graphql';

  // token name
  static const token = 'PERSONAL_ACCESS_TOKEN';

  // font
  static const fontFamily = 'One UI Sans';
}